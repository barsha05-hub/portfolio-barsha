services:
  - type: web
    name: cinematic-portfolio
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false
